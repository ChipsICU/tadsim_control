## install
```
$ sudo apt-get update
$ sudo apt-get install libboost-all-dev # boost
$ sudo apt-get install protobuf-compiler # protobuf
$ sudo apt-get install libtinyxml2-dev # tinyxml2
$ sudo apt install libeigen3-dev # eigen3

```
## complie
```
$ mkdir build && cd build
$ cmake ..
$ make
```
